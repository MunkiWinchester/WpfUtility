<UserControl x:Class="Sample.UserControls.UserControl1"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             xmlns:guc="clr-namespace:WpfUtility.GeneralUserControls;assembly=WpfUtility"
             xmlns:wpfUtility="clr-namespace:WpfUtility;assembly=WpfUtility"
             d:DesignHeight="300" d:DesignWidth="300">
    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter" />
        <wpfUtility:NegatedBooleanToVisibilityConverter x:Key="NegatedBooleanToVisibilityConverter" />
    </UserControl.Resources>
    <StackPanel>
        <CheckBox x:Name="CheckBox" />
        <guc:CircularLoadingAnimation2 ForegroundColor="Red" Visibility="{Binding ElementName=CheckBox, Path=IsChecked, Converter={StaticResource NegatedBooleanToVisibilityConverter}}" />
    </StackPanel>
</UserControl>