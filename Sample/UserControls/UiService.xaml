<UserControl x:Class="Sample.UserControls.UiService"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:guc="clr-namespace:WpfUtility.GeneralUserControls;assembly=WpfUtility"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid>
        <DockPanel HorizontalAlignment="Center" VerticalAlignment="Top">
            <StackPanel Orientation="Horizontal" Margin="5">
                <Button x:Name="BtnShow" Content="Show Panel" Command="{Binding ShowPanelCommand}"
                Click="ButtonBase_OnClick" Width="86" Height="22" />
                <Button x:Name="BtnHide" Content="Hide Panel" Command="{Binding HidePanelCommand}"
                Click="ButtonBase_OnClick" Width="86" Height="22" Margin="5,0,0,0" />
                <Button x:Name="BtnChange" Content="Change Sub Message" Command="{Binding ChangeSubMessageCommand}"
                Width="130" Height="22" Margin="5,0,0,0" />
            </StackPanel>
        </DockPanel>
        <guc:LoadingPanel x:Name="LoadingPanel" IsLoading="{Binding PanelLoading}" Message="{Binding PanelMainMessage}"
                        SubMessage="{Binding PanelSubMessage}" ClosePanelCommand="{Binding PanelCloseCommand}" />
    </Grid>
</UserControl>
